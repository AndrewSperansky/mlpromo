
## Проверка удаленного репозитория:
git remote -v 


## проверить user.name, user.email, remote.origin.url
git config --list  

## Статус репозитория:

git status
git log --oneline --graph --all  # визуализация истории


## Подключаем локальный проект

git init
git add .
git commit -m "Initial commit: ML Promo backend"
git branch -M main
git remote add origin https://github.com/AndrewSperansky/mlpromo.git
git push -u origin main

## Создаём новую ветку для фичи или бага и сразу переходим в нее

`git checkout -b feature/<имя_фичи>`


## Удалить ненужную ветку (смерженную)  
git branch -d stage2

### Если Git вдруг скажет, что есть несмёрженные коммиты
git branch -D stage2

## Удаляем удалённую ветку (если она уже была пушена)
git push origin --delete stage2




## Привязываем локальный проект к GitHub

git remote add origin https://github.com/AndrewSperansky/mlpromo.git

### проверяем
git remote -v


### Должно появиться
origin  https://github.com/AndrewSperansky/3dsonet.git (fetch)
origin  https://github.com/AndrewSperansky/3dsonet.git (push)





## заменить origin на правильный URL
git remote set-url origin https://github.com/AndrewSperansky/3dsonet.git


## Работа с тэгами

### Создание и отправка на GitHub
git tag stage2-demo-ready  
git push origin stage2-demo-ready

Тэг привязывается к текущему коммиту!

### Посмотреть тэги
git tag       




1️⃣ Основные ветки

| Ветка       | Назначение                                       |
| ----------- | ------------------------------------------------ |
| `main`      | Продакшн, всегда стабильная версия               |
| `develop`   | Интеграция новых фич, тестовая ветка перед merge |
| `feature/*` | Новые функции (например, `feature/auth`)         |
| `fix/*`     | Исправления багов (например, `fix/posts-crash`)  |
| `hotfix/*`  | Срочные исправления на продакшн                  |


2️⃣ Рабочий процесс (Git Flow)

Обновляем develop/main перед работой

# Сначала на develop
git checkout develop
git pull origin develop

# Или на main
git checkout main
git pull origin main


## Создаём новую ветку для фичи или бага и сразу переходим в нее

`git checkout -b feature/<имя_фичи>`

## Просто переходим на ветку

`git checkout  service`

## Узнать в какой ветке работаем

`git branch`
`git status`
`git branch --show-current`

## Удалить ветку

`git branch -d service`   Safe delete     
`git branch -D service`   Force delete

Пример:

git checkout -b feature/jwt-auth


Работаем над кодом, коммитим часто:

git add .
git commit -m "feat(auth): add JWT login endpoint"


## Подтягиваем изменения develop/main перед merge

git fetch origin
git rebase origin/develop


## Если есть конфликты .gitignore или других файлов — правим в VSCode / Notepad, затем:

git add <исправленный_файл>
git rebase --continue


## Сливаем ветку фичи в develop

git checkout develop
git merge --no-ff feature/jwt-auth
git push origin develop


## Используем --no-ff, чтобы история ветки сохранилась.

## Сливаем develop в main для продакшн

git checkout main
git pull origin main
git merge --no-ff develop
git push origin main

3️⃣ Быстрый fix на продакшн (hotfix)
git checkout -b hotfix/<имя_исправления> main

# исправляем баг
git add .
git commit -m "fix(auth): correct token expiration"
git push origin hotfix/<имя_исправления>

# сливаем обратно в main и develop
git checkout main
git merge --no-ff hotfix/<имя_исправления>
git push origin main

git checkout develop
git merge --no-ff hotfix/<имя_исправления>
git push origin develop

4️⃣ Важные команды для контроля

## Проверка состояния ветки:

git status


## Список всех веток:

git branch -a


## Просмотр истории коммитов:

git log --oneline --graph --all


## Отмена незакоммиченных изменений:

git restore .


## Отмена последнего коммита (не push):

git reset --soft HEAD~1


## Что такое Staged Changes

### Staged Changes (или индекс) — это изменения, которые готовы для следующего коммита.

## В Git есть три состояния файла:

Состояние	Объяснение
1️⃣Modified	Файл изменён в рабочей директории, но Git ещё не знает, что с ним делать
2️⃣Staged	Файл добавлен в индекс (git add), готов к коммиту
3️⃣Committed	Изменение сохранено в истории Git

Пример:

## Изменили файл src/app.module.ts
git status
## Вывод: modified: src/app.module.ts

## Добавляем его в staged
git add src/app.module.ts
git status
## Теперь он в staged
## staged changes: src/app.module.ts

## Делаем коммит
git commit -m "feat: add new module"


## Как это выглядит в Git Graph

### Git Graph показывает три уровня:

1️⃣Working Directory / Modified — изменения в файлах, которые ещё не добавлены в индекс.
2️⃣Staged Changes — изменения, которые уже добавлены через git add.
3️⃣Commit History — уже сохранённые коммиты.

#### То есть staged — это промежуточный шаг между редактированием и коммитом.

## Почему staged нужен

## Позволяет выбрать, какие изменения пойдут в коммит, а какие пока нет.
## Можно коммитить частями, даже если несколько файлов изменены.

### Пример:

git add src/auth/auth.service.ts
git commit -m "feat: add login logic"

## Только этот файл будет в коммите, остальные остаются modified

## Удаление файла

git rm --cached src/.gitignore  # если он уже под Git
del src\.gitignore              # удалить физически

## Добавление файла

git add .gitignore
git commit -m "chore: remove duplicate .gitignore from src/"
git push origin main

## ОСНОВНЫЕ КОМАНДЫ

| Действие                                         | Команда                                   | Пояснение                                                     |
| ------------------------------------------------ | ----------------------------------------- | ------------------------------------------------------------- |
| **Проверка состояния**                           | `git status`                              | Показывает изменённые, staged и неотслеживаемые файлы         |
| **Добавить файлы в staged**                      | `git add <файл>` или `git add .`          | Подготовить изменения к коммиту                               |
| **Коммит с сообщением**                          | `git commit -m "сообщение"`               | Сохраняет staged изменения в истории                          |
| **Просмотр истории**                             | `git log --oneline --graph --all`         | Краткая графическая история всех коммитов                     |
| **Переключение ветки**                           | `git checkout <ветка>`                    | Перейти на существующую ветку                                 |
| **Создание ветки**                               | `git checkout -b <ветка>`                 | Создать новую ветку и сразу перейти на неё                    |
| **Слияние ветки**                                | `git merge <ветка>`                       | Влить указанную ветку в текущую                               |
| **Пересборка ветки (rebase)**                    | `git rebase <ветка>`                      | Переписать историю текущей ветки на основе другой ветки       |
| **Пропустить коммит при rebase**                 | `git rebase --skip`                       | Использовать, если коммит не нужен или конфликт решён вручную |
| **Продолжить rebase после конфликта**            | `git rebase --continue`                   | После исправления конфликтов                                  |
| **Отменить rebase**                              | `git rebase --abort`                      | Вернуться к состоянию до начала rebase                        |
| **Удаление файла из Git (не с диска)**           | `git rm --cached <файл>`                  | Убирает из репозитория, но оставляет локально                 |
| **Отмена изменений в файле**                     | `git restore <файл>`                      | Отменяет изменения в рабочей директории                       |
| **Отмена staged изменений**                      | `git restore --staged <файл>`             | Убирает файл из staged, но оставляет изменения                |
| **Подтянуть изменения с удалённого репозитория** | `git pull origin <ветка>`                 | Скачивает и сливает изменения с GitHub                        |
| **Отправка коммитов на GitHub**                  | `git push origin <ветка>`                 | Отправляет локальные изменения на удалённый репозиторий       |
| **Список всех веток**                            | `git branch -a`                           | Показывает локальные и удалённые ветки                        |
| **Создание и применение тега**                   | `git tag <имя>` и `git push origin <имя>` | Используется для отметки версий                               |
| **Проверка удалённого репозитория**              | `git remote -v`                           | Показывает URL GitHub репозитория                             |
| **Игнорирование файлов**                         | `.gitignore`                              | Правила для исключения файлов из Git                          |


# РАСШИРЕННАЯ ТАБЛИЦА

| Действие                                         | Команда                                 | Пояснение                                                      |
| ------------------------------------------------ | --------------------------------------- | -------------------------------------------------------------- |
| **Создать новую ветку**                          | `git checkout -b <ветка>`               | Создать ветку и сразу перейти на неё                           |
| **Переключение ветки**                           | `git checkout <ветка>`                  | Перейти на существующую ветку                                  |
| **Слияние ветки**                                | `git merge <ветка>`                     | Влить указанную ветку в текущую                                |
| **Rebase текущей ветки на другую**               | `git rebase <ветка>`                    | Переписать историю текущей ветки на основе другой              |
| **Продолжить rebase после конфликта**            | `git rebase --continue`                 | После исправления конфликтов                                   |
| **Пропустить коммит при rebase**                 | `git rebase --skip`                     | Если коммит не нужен или конфликт решён вручную                |
| **Отменить rebase**                              | `git rebase --abort`                    | Вернуться к состоянию до начала rebase                         |
| **Проверить состояние репозитория**              | `git status`                            | Показывает modified, staged и untracked файлы                  |
| **Добавить файлы в staged**                      | `git add <файл>` или `git add .`        | Подготовить изменения к коммиту                                |
| **Убрать файл из staged**                        | `git restore --staged <файл>`           | Файл остаётся изменённым, но убирается из staged               |
| **Отменить изменения в файле**                   | `git restore <файл>`                    | Возвращает файл к последнему коммиту                           |
| **Коммит изменений**                             | `git commit -m "сообщение"`             | Сохраняет staged изменения в истории                           |
| **Просмотр истории коммитов**                    | `git log --oneline --graph --all`       | Краткая графическая история всех коммитов                      |
| **Временное сохранение изменений (stash)**       | `git stash`                             | Сохраняет изменения, чтобы переключиться на другую ветку       |
| **Применение сохранённого stash**                | `git stash apply`                       | Восстанавливает изменения из stash                             |
| **Просмотр списка stash**                        | `git stash list`                        | Показать все сохранённые изменения                             |
| **Удаление stash после применения**              | `git stash pop`                         | Восстанавливает изменения и удаляет их из stash                |
| **Подтянуть изменения с удалённого репозитория** | `git pull origin <ветка>`               | Скачивает и сливает изменения с GitHub                         |
| **Отправить коммиты на GitHub**                  | `git push origin <ветка>`               | Отправляет локальные изменения на удалённый репозиторий        |
| **Удаление файла из Git (не с диска)**           | `git rm --cached <файл>`                | Убирает из репозитория, но оставляет локально                  |
| **Список всех веток**                            | `git branch -a`                         | Показывает локальные и удалённые ветки                         |
| **Создание тега**                                | `git tag <имя>`                         | Отметка версии                                                 |
| **Отправка тега на GitHub**                      | `git push origin <имя>`                 | Отправка тега в удалённый репозиторий                          |
| **Проверка удалённого репозитория**              | `git remote -v`                         | Показывает URL GitHub репозитория                              |
| **Игнорирование файлов**                         | `.gitignore`                            | Правила для исключения файлов из Git                           |
| **Разрешение конфликтов**                        | вручную правим файлы + `git add <файл>` | После этого продолжаем `git rebase --continue` или `git merge` |
| **Интерактивный rebase**                         | `git rebase -i <коммит>`                | Можно изменить, объединить или пропустить коммиты              |


# ТАБЛИЦА БЫСТРАЯ


| Действие                            | Команда                           | Пояснение                                         |
| ----------------------------------- | --------------------------------- | ------------------------------------------------- |
| Проверка состояния                  | `git status`                      | Узнать, что изменилось и что staged               |
| Добавить изменения                  | `git add <файл>` или `git add .`  | Подготовить файлы к коммиту                       |
| Коммит                              | `git commit -m "сообщение"`       | Сохранить изменения в истории                     |
| Отправка на GitHub                  | `git push origin <ветка>`         | Отправить локальные изменения на сервер           |
| Подтянуть изменения                 | `git pull origin <ветка>`         | Получить обновления с GitHub и объединить их      |
| Создать/перейти на ветку            | `git checkout -b <ветка>`         | Создать новую ветку и сразу перейти на неё        |
| Переключиться на существующую ветку | `git checkout <ветка>`            | Перейти на нужную ветку                           |
| Слияние ветки                       | `git merge <ветка>`               | Влить указанную ветку в текущую                   |
| Rebase                              | `git rebase <ветка>`              | Переписать историю текущей ветки на основе другой |
| Продолжить rebase                   | `git rebase --continue`           | После решения конфликтов                          |
| Пропустить коммит при rebase        | `git rebase --skip`               | Если коммит не нужен или конфликт решён вручную   |
| Отменить изменения в файле          | `git restore <файл>`              | Вернуть файл к последнему коммиту                 |
| Убрать staged изменения             | `git restore --staged <файл>`     | Файл остаётся изменённым, но убирается из staged  |
| Временное сохранение изменений      | `git stash`                       | Спрятать текущие изменения                        |
| Применить stash                     | `git stash apply`                 | Восстановить спрятанные изменения                 |
| Быстрый просмотр коммитов           | `git log --oneline --graph --all` | Графическая история всех коммитов                 |



# ТАБЛИЦА НА КАЖДЫЙ ЧАС


| Действие                            | Команда                           | Пояснение                                         |
| ----------------------------------- | --------------------------------- | ------------------------------------------------- |
| Проверка состояния                  | `git status`                      | Узнать, что изменилось и что staged               |
| Добавить изменения                  | `git add <файл>` или `git add .`  | Подготовить файлы к коммиту                       |
| Коммит                              | `git commit -m "сообщение"`       | Сохранить изменения в истории                     |
| Отправка на GitHub                  | `git push origin <ветка>`         | Отправить локальные изменения на сервер           |





# 🔹 Простыми словами

## Stash позволяет временно спрятать изменения в рабочей директории и staged файлах, чтобы переключиться на другую ветку или сделать срочный коммит, не теряя текущую работу.

## После этого рабочая директория возвращается к состоянию последнего коммита, будто ты ничего не менял.

🔹 Основные команды


| Действие                  | Команда                  | Пояснение                                                      |
| ------------------------- | ------------------------ | -------------------------------------------------------------- |
| Спрятать изменения        | `git stash`              | Сохраняет все изменения (staged и unstaged) во временный stash |
| Список stash              | `git stash list`         | Показать все сохранённые тайники                               |
| Применить stash           | `git stash apply`        | Восстанавливает изменения из stash, stash остаётся             |
| Применить и удалить stash | `git stash pop`          | Восстанавливает изменения и удаляет их из stash                |
| Удалить stash             | `git stash drop <stash>` | Удалить конкретный stash по имени                              |
| Очистить все stash        | `git stash clear`        | Удалить все тайники                                            |




## 🔹 Пример использования

## Ты работаешь над comments сервисом, но срочно нужно переключиться на main:

git stash          # прячем изменения
git checkout main  # переключаемся на main


## Решил срочную задачу на main, теперь возвращаемся к работе над сервисом:

git checkout feature/comments
git stash pop      # возвращаем изменения обратно


## ✅ Всё, что было спрятано, вернулось на место.



# наглядная мини-схема работы Git Stash — максимально простая и понятная:

┌────────────────────┐
│  Working Directory │   ← ты что-то изменил
└───────────┬────────┘
            │  git stash
            ▼
     ┌──────────────┐
     │    STASH     │  ← изменения спрятаны
     │ (тайник Git) │
     └──────┬───────┘
            │  git stash pop / apply
            ▼
┌────────────────────┐
│  Working Directory │  ← изменения возвращены
└────────────────────┘


## 🚀 Коротко логика процесса
### 1️⃣ Ты сделал изменения →

## Git видит modified / added файлы.

### 2️⃣ git stash →

## Изменения переносятся в stash
Рабочая директория становится «чистой», как будто только что сделал git pull.

### 3️⃣ Работаешь с ветками как нужно →

Переключаешься, делаешь фиксы, коммиты и т.п.

### 4️⃣ git stash pop →

Изменения возвращаются обратно
(и stash удаляется)


## 🔵 1. Что такое git rebase
### 📌 Смысл:

### Переписать историю своей ветки так, будто она была создана позже.
Т### ты как бы “перено́сишь” свои коммиты поверх актуальной версии другой ветки.

🔧 Пример: git rebase main
До rebase
(main) A──B──C
          \
(feature)  D──E


## После:

### После rebase feature на main

(main)    A──B──C
                   \
(feature new)       D'──E'


### D' и E’ — те же изменения, но как новые коммиты.


## Принудительно перезаписать удалённую ветку (правильный и безопасный)
git push -u origin main --force


## если хочешь сохранить тот один коммит с GitHub
git pull origin main --rebase
git push -u origin main

git fetch origin
git pull --rebase origin main
# решить конфликты если есть
git push origin main





🔵 2. Что такое HEAD
📌 Смысл:

HEAD — это указатель на текущий коммит, где ты сейчас работаешь.

Схема:
Коммиты:

A──B──C──D
        ▲
       HEAD  ← ты находишься на коммите D

Если переключишь ветку:
git checkout feature

A──B──C──D    ← ветка main
      \
       E──F   ← ветка feature
           ▲
          HEAD


HEAD просто переезжает туда, где ты работаешь.

🔵 3. Что такое origin
📌 Смысл:

origin — это имя удалённого репозитория, обычно GitHub.

Когда ты клонируешь проект:

GitHub repo  ← имя: origin
      │
      ▼
Local clone

Типичное отображение:
origin/main     — удалённая ветка на GitHub  
main            — твоя локальная ветка  
HEAD → main     — ты работаешь в локальной main

Когда ты делаешь push:
Локальная ветка main
            │ git push
            ▼
Удалённая ветка origin/main



## Переключаемся в ветку main
git checkout main  
git pull origin main  # синхронизация с удалённым репозиторием
git merge bizlogic  # слияние bizlogic в текущую ветку (main)
git tag stage1     # создание тега stage1 на текущем коммите

git checkout -b promo-ml/stage2  # создание новой ветки и переход в неё

git push origin main           # обновление удалённой ветки main
git push origin promo-ml/stage2  # отправка новой ветки
git push --tags               # отправка тега stage1


